package pages;

import org.apache.logging.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class JavaScriptErrorsPage extends BasePageObject {

    public JavaScriptErrorsPage(WebDriver driver, Logger log) {
        super(driver, log);
    }
}
